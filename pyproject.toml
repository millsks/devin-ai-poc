[project]
name = "devin-ai-poc"
version = "0.1.0"
description = "Documentation site for devin-ai-poc"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
docs = [
    "mkdocs>=1.5.3",
    "mkdocs-material>=9.5.0",
    "mkdocs-material-extensions>=1.3.1",
    "pymdown-extensions>=10.7",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
